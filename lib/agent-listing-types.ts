/** Persisted approved listing — file or Neon row shape */
export type ApprovedListingStored = {
  slug: string;
  name: string;
  tagline: string;
  category: string;
  installsLabel: string;
  rating: number;
  author: string;
  applicationId: string;
  agentApiUrl: string;
  approvedAt: string;
};
